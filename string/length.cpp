#include<iostream>
using namespace std;

int main()
{
   string name = "Animesh";  //Declration of string
   // char *name1 = "Animeshb"; //We should use string in c++ not char because it shows error
    // char name[]="Animesh"; it is write because we creating array of charcter .
    int i;
    for(i=0;name[i]!=0;i++) //name[i]!=0 means till then we have to find length.
    {
           
    }
    cout<<i<<endl;
   
}